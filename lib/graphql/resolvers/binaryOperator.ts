type Problem = {
  problem: string,
  solution: string
}

type ProblemBlueprint = {
  operands: string[],
  operator: string,
  links: string[],
  number: number
}

// const getProblems = (params: ProblemBlueprint): Problem[] => {

// };

// export default (args): Problem => {
//   return ;
// };
